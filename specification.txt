Hyphy -- A Rails performance testing framework

Requirements:
  - Samples all SQL calls
  - Logs length of time
  - Can classify "similar" SQL statements, and nest classifications
  - Provide good interface so analysis can be made

Implementation:
  - Use SQLite3
  - Use sequel

Todo:
  - Create reports
  - Save query plan
  - Run queries multiple times to see if caching makes a difference
  - Filter out updates and inserts
  - Query plan

Filters
  - Nation
  - Query time
  - Limit
  - Cache benchmark
  - Query base
  - Metadata
